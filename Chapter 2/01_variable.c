#include <stdio.h>

int main(){
    int i = 10;
    int j = 20;

    //%d is used to print integer & %f is used to print float
    printf("Value of i is %d\n & value of j is %d\n", i , j);

    return 0;
}